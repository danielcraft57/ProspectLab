import { Stack } from 'expo-router';

/** Pile liste → détail campagne (retour Android cohérent). */
export default function CampagnesStackLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
